      <%= image_tag @tweet.image.variant(resize: '240*240'), class:   "music_image" if @tweet.image.attached? %>
      <%= form.text_area :text, placeholder: "text", rows: "10" %>
      <%= form.submit "SEND" %>